# Changelog | 更新紀錄

所有重要變更紀錄。格式依循 [Keep a Changelog](https://keepachangelog.com/)。

---

## [Unreleased]

### 白皮書對齊工程 v1（套件預覽版本 0.2.0b1，2026-09-06）

- 新增 OpenAI Chat Completions 相容 transport、後端 factory、明確 offline 選項。
- 輸出前逐行 A/B/C 與來源審核；年份不再被當作具體來源。保留 raw result 與 audit。
- 最終提案所有重寫留在 ATL-3 最多三次生成內，避免驗收後又生成未驗收版本。
- 生產流程傳輸最多 30 筆 / 9,000 字元；記錄省略端點、待確認端點與已知／未知分類。
- /check 執行本地查表並記錄，但不向內容查核模型注入私人歷史。
- /memory confirm / supersede 明確裁決舊記憶，保留狀態歷史；production mark_hit 不再自動恢復。
- 偏好 NFKC / casefold / 空白正規化去重，保留觀察證據與否定類型。
- The Killers 英文子字串誤判修正；仍不宣稱完成語意風險偵測。
- 加入 pyproject、環境範本、CLI 入口與 CI；文件更正儲存／傳輸、歷史數據與驗證邊界。

既有未發布修改保留如下。


### 修正（止血三連修，施工單 2026-08-07）

- **修復一／補位方向**：自帶率改為三態語義
  `answered / (gap + answered)`——十次都被補問的面向不再被判成
  「強項，不需要問」；從未出現的面向標為 unknown，不進策略摘要；
  樣本 < 3 不下判斷。補位回答涵蓋的維度（`answered_categories`）
  自 v0.9 起隨端點落盤。策略摘要措辭改為「目前觀察到」。
- **修復二／記憶保真**：密碼表 `GOAL` 欄位改存使用者原話
  （「我就直接取消」不再被改寫成「完成爬山活動」）；逗福的詮釋
  保留在新增的 `DERIVED` 欄位並於 CODEBOOK 標明「非使用者原話、
  衝突時以 GOAL 為準」。實測 50 對端點密碼表增幅 +21.3%（< 30% 門檻）。
- **修復三／執行階段可見原話**：`execute_task` 與 `analyze_deviation`
  新增 `raw_user_input`（後者含格式限制豁免——使用者要求簡短的正常
  回覆不算偏差）、`execute_task` 另增 `confirmed_understanding`，
  prompt 明示證據優先序（原話 > 確認過的理解 > 結構化欄位）。
  未傳入時 prompt 與舊版完全一致，舊簽名 mock 以 TypeError 退化相容。

### 修正（畫像提取三連修，SAV 實測發現）

- **繁中斷詞**：內建 jieba 官方繁簡合一大詞典（`resources/jieba/dict.txt.big`，
  MIT），修正簡體詞典切繁體產生的跨詞界碎片（「這禮/拜」「電費帳/單寄來」）。
  可用環境變數 `TOFU_JIEBA_DICT` 覆寫或停用（設 `default`）。
- **偏好誤匹配**：中文排除指標詞加上邊界防護——「媽問我要不要換車」不再
  因子字串命中「不要」而被存成排除偏好（同理「不要緊」「想不想」「喜不喜歡」）。
- **排除偏好語義自足**：exclusion 的 item 保留否定詞本身（存「避免踩到箱子」
  而非「踩到箱子」），不再依賴下游記得看 `[exclusion]` 標記。
- **興趣領域過濾**：domains 改用詞性標註只留名詞性詞，「放在」「這樣」「突然」
  這類功能詞不再佔滿榜單。
- 新增 `eval/sav/diagnose_extraction.py`：掃 endpoints.jsonl 驗屍提取品質，
  可用於回掃歷史資料的誤匹配佔比。

實測（50 輪敘事語料）：有效偏好 0 → 2 條、domains 前 20 名虛詞從八成降至
近零。已知限制：關鍵詞掃描對敘事式偏好的覆蓋天花板低（2/44 條訊號），
提升覆蓋需要 LLM 側的畫像建構，屬後續工作。

---

## [0.1.0-beta] — 2026-05-23

### 🎉 首次公開發布（技術預覽版）

**核心功能**
- 復述確認引擎 — 每次互動先用自己的話復述使用者需求
- 七維度補位提問 — 主動帶出使用者沒想到的關鍵面向
- 端點紀錄系統 — 只存起點和終點，不存過程，儲存量收斂
- 使用者畫像引擎 — 越用越懂你的思考模式與盲區
- 邏輯基線統計 — 追蹤高頻目標與常被忽略的面向
- 品質三層標記 — 事實／推測／建議自動分類
- 安全阻斷（CIP-X）— 趨勢偵測式阻斷，非關鍵字過濾
- ATL 三重檢查 — 具體性檢測、反駁條件、來源可回溯
- 收斂閘設計 — 連續空回合自動止損
- 離線 fallback 模式 — 未設定 API key 時自動啟用

**測試**
- 608 項自動化測試全部通過
- 244 筆實測互動，零錯誤，總費用 US$2.57

**支援**
- 內建 Claude Haiku（推薦）、Claude Opus 後端；採模型中立架構（BaseLLMClient），可自行串接其他模型
- jieba 中文分詞（選裝）
- Windows / macOS / Linux

---

## 開發歷史

逗福Tofu 從 2024 年開始設計，經歷 18 個主要版本迭代、多次架構重構。
詳細開發過程見 `docs/` 資料夾（即將公開）。
